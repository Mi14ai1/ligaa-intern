mixin loader()
  .loader(data-loader="container")
    .loader__box
      .loader__box-wrapper(data-loader="box")
        .loader__box-item
        .loader__box-item
        .loader__box-item
        .loader__box-item
